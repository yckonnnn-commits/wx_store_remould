import React, { useState } from 'react';
import { 
  Layout, 
  Settings, 
  Image as ImageIcon, 
  BookOpen, 
  ShoppingBag, 
  Play, 
  Square, 
  RefreshCw, 
  Search, 
  Upload, 
  Trash2, 
  CheckCircle2, 
  ChevronRight, 
  Maximize2, 
  Terminal, 
  Grid, 
  Target, 
  Plus, 
  Download, 
  UploadCloud, 
  Edit3, 
  Eye, 
  EyeOff, 
  Save, 
  ExternalLink,
  Cpu,
  Globe
} from 'lucide-react';

const App = () => {
  const [activeTab, setActiveTab] = useState('shop'); // 默认进入微信小店
  const [isRunning, setIsRunning] = useState(false);
  const [selectedImages, setSelectedImages] = useState([]);
  const [previewImage, setPreviewImage] = useState(null);
  const [showApiKey, setShowApiKey] = useState({});

  // 模拟数据：知识库
  const knowledgeData = [
    { id: 1, category: "购买方式", tags: ["售前话术", "引导私域"], question: "怎么买？如何买？在哪里买？", answer: "姐姐，我们在上海和北京都有线下店，您可以..." },
    { id: 2, category: "地址门店", tags: ["售前话术", "位置信息"], question: "地址在哪？店铺地址？门店地址？", answer: "姐姐我们的地址受平台管控无法直接发送，建议..." },
    { id: 3, category: "选购建议", tags: ["产品介绍", "假发选择"], question: "怎么选择假发？如何选？", answer: "姐姐选假发主要看头围、脸型、肤色和..." },
    { id: 4, category: "品牌介绍", tags: ["品牌优势"], question: "你们是做什么的？品牌优势？", answer: "我们不只是卖假发，更是为您量身定制自信..." },
  ];

  // 模拟数据：模型配置
  const models = [
    { id: 'deepseek', name: 'DeepSeek', icon: <Cpu className="w-5 h-5" />, active: true, url: 'https://api.deepseek.com/v1', modelName: 'deepseek-v3' },
    { id: 'chatgpt', name: 'ChatGPT', icon: <Globe className="w-5 h-5" />, active: false, url: 'https://api.openai.com/v1', modelName: 'gpt-4o-mini' },
    { id: 'gemini', name: 'Gemini', icon: <Target className="w-5 h-5" />, active: false, url: 'https://generativelanguage.googleapis.com', modelName: 'gemini-1.5-flash' },
  ];

  const toggleApiKey = (id) => {
    setShowApiKey(prev => ({ ...prev, [id]: !prev[id] }));
  };

  // --- 子组件：微信小店 (内嵌网页) ---
  const ShopView = () => (
    <div className="flex flex-col h-full bg-slate-100">
      <div className="bg-white px-4 py-2 border-b border-slate-200 flex items-center gap-4">
        <div className="flex gap-1.5">
          <div className="w-3 h-3 rounded-full bg-red-400"></div>
          <div className="w-3 h-3 rounded-full bg-yellow-400"></div>
          <div className="w-3 h-3 rounded-full bg-green-400"></div>
        </div>
        <div className="flex-1 bg-slate-100 rounded-lg py-1.5 px-4 flex items-center gap-2 text-xs text-slate-500 border border-slate-200/50">
          <Globe className="w-3 h-3" />
          <span>https://store.weixin.qq.com/shop/kf</span>
        </div>
        <button className="p-2 hover:bg-slate-100 rounded-lg text-slate-500"><RefreshCw className="w-4 h-4" /></button>
      </div>
      <div className="flex-1 bg-white m-4 rounded-2xl shadow-inner border border-slate-200 flex items-center justify-center overflow-hidden">
         {/* 模拟内嵌内容 */}
         <div className="flex flex-col items-center opacity-40">
            <ShoppingBag className="w-16 h-16 mb-4 text-slate-300" />
            <p className="text-slate-500 font-medium">此处为内嵌微信小店客服网页内容</p>
            <p className="text-xs text-slate-400 mt-1">WebView 内容加载中...</p>
         </div>
      </div>
    </div>
  );

  // --- 子组件：知识库管理 ---
  const KnowledgeView = () => (
    <div className="p-8 flex flex-col h-full overflow-hidden">
      <div className="flex items-center justify-between mb-8">
        <div>
          <h2 className="text-2xl font-bold text-slate-800">知识库管理</h2>
          <p className="text-slate-500 text-sm">维护 AI 的回复逻辑与业务话术 (共 {knowledgeData.length} 条数据)</p>
        </div>
        <div className="flex gap-3">
          <button className="flex items-center gap-2 bg-slate-100 text-slate-700 px-4 py-2 rounded-xl text-sm font-semibold hover:bg-slate-200 border border-slate-200 transition-all">
            <Download className="w-4 h-4" /> 导出
          </button>
          <button className="flex items-center gap-2 bg-slate-100 text-slate-700 px-4 py-2 rounded-xl text-sm font-semibold hover:bg-slate-200 border border-slate-200 transition-all">
            <UploadCloud className="w-4 h-4" /> 批量导入
          </button>
          <button className="flex items-center gap-2 bg-blue-600 text-white px-5 py-2 rounded-xl text-sm font-semibold hover:bg-blue-700 shadow-lg shadow-blue-100">
            <Plus className="w-4 h-4" /> 添加话术
          </button>
        </div>
      </div>

      <div className="bg-white rounded-2xl border border-slate-200 shadow-sm overflow-hidden flex flex-col flex-1">
        <div className="p-4 border-b border-slate-100 bg-slate-50/50 flex items-center gap-4">
          <div className="relative flex-1">
            <Search className="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-slate-400" />
            <input type="text" placeholder="搜索关键词、标签或问题..." className="w-full bg-white border border-slate-200 rounded-xl py-2 pl-10 pr-4 text-sm focus:ring-2 focus:ring-blue-500 outline-none transition-all" />
          </div>
        </div>

        <div className="overflow-y-auto flex-1">
          <table className="w-full text-left border-collapse">
            <thead className="sticky top-0 bg-white shadow-sm z-10 text-[11px] uppercase tracking-wider text-slate-400 font-bold border-b border-slate-100">
              <tr>
                <th className="px-6 py-4">分类</th>
                <th className="px-6 py-4">标签</th>
                <th className="px-6 py-4">核心问题</th>
                <th className="px-6 py-4">预览答案</th>
                <th className="px-6 py-4 text-right">操作</th>
              </tr>
            </thead>
            <tbody className="text-sm divide-y divide-slate-50">
              {knowledgeData.map(item => (
                <tr key={item.id} className="hover:bg-blue-50/30 transition-colors group">
                  <td className="px-6 py-4">
                    <span className="px-2.5 py-1 bg-blue-50 text-blue-600 rounded-lg text-xs font-semibold">{item.category}</span>
                  </td>
                  <td className="px-6 py-4">
                    <div className="flex gap-1.5 flex-wrap">
                      {item.tags.map((tag, i) => (
                        <span key={i} className="px-2 py-0.5 bg-slate-100 text-slate-500 rounded-md text-[10px] font-medium">{tag}</span>
                      ))}
                    </div>
                  </td>
                  <td className="px-6 py-4 font-medium text-slate-700">{item.question}</td>
                  <td className="px-6 py-4 text-slate-500 max-w-xs truncate">{item.answer}</td>
                  <td className="px-6 py-4 text-right">
                    <div className="flex justify-end gap-2 opacity-0 group-hover:opacity-100 transition-opacity">
                      <button className="p-2 hover:bg-white rounded-lg border border-transparent hover:border-slate-200 text-slate-400 hover:text-blue-600"><Edit3 className="w-4 h-4" /></button>
                      <button className="p-2 hover:bg-white rounded-lg border border-transparent hover:border-slate-200 text-slate-400 hover:text-red-600"><Trash2 className="w-4 h-4" /></button>
                    </div>
                  </td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      </div>
    </div>
  );

  // --- 子组件：模型配置 ---
  const ModelConfigView = () => (
    <div className="p-8 flex flex-col h-full overflow-y-auto">
      <div className="flex items-center justify-between mb-8">
        <div>
          <h2 className="text-2xl font-bold text-slate-800">AI 模型配置</h2>
          <p className="text-slate-500 text-sm">管理大模型 API 密钥与端点，支持多引擎切换</p>
        </div>
        <button className="flex items-center gap-2 bg-blue-600 text-white px-6 py-2.5 rounded-xl font-bold hover:bg-blue-700 shadow-lg shadow-blue-100">
          <Save className="w-4 h-4" /> 保存全局配置
        </button>
      </div>

      <div className="grid grid-cols-1 gap-6 max-w-5xl">
        {models.map((model) => (
          <div key={model.id} className={`bg-white rounded-2xl border-2 transition-all p-6 ${model.active ? 'border-blue-500 shadow-md ring-4 ring-blue-50' : 'border-slate-100 shadow-sm opacity-80'}`}>
            <div className="flex items-center justify-between mb-6">
              <div className="flex items-center gap-4">
                <div className={`p-3 rounded-xl ${model.active ? 'bg-blue-500 text-white' : 'bg-slate-100 text-slate-400'}`}>
                  {model.icon}
                </div>
                <div>
                  <h3 className="font-bold text-lg text-slate-800">{model.name}</h3>
                  <div className="flex items-center gap-2">
                    <span className={`w-2 h-2 rounded-full ${model.active ? 'bg-green-500' : 'bg-slate-300'}`}></span>
                    <span className="text-xs text-slate-400">{model.active ? '当前使用模型' : '待命'}</span>
                  </div>
                </div>
              </div>
              {!model.active && <button className="text-sm font-semibold text-blue-600 hover:underline">切换到此模型</button>}
            </div>

            <div className="grid grid-cols-2 gap-x-8 gap-y-6">
              <div className="space-y-2">
                <label className="text-[10px] uppercase font-bold text-slate-400 tracking-wider">API 地址 (Base URL)</label>
                <input type="text" defaultValue={model.url} className="w-full bg-slate-50 border border-slate-200 rounded-xl px-4 py-2.5 text-sm focus:ring-2 focus:ring-blue-500 outline-none" />
              </div>
              <div className="space-y-2">
                <label className="text-[10px] uppercase font-bold text-slate-400 tracking-wider">模型名称 (Model ID)</label>
                <input type="text" defaultValue={model.modelName} className="w-full bg-slate-50 border border-slate-200 rounded-xl px-4 py-2.5 text-sm focus:ring-2 focus:ring-blue-500 outline-none" />
              </div>
              <div className="space-y-2 col-span-2 relative">
                <label className="text-[10px] uppercase font-bold text-slate-400 tracking-wider">API 密钥 (API Key)</label>
                <div className="relative">
                  <input 
                    type={showApiKey[model.id] ? "text" : "password"} 
                    defaultValue="sk-xxxxxxxxxxxxxxxxxxxxxxxx" 
                    className="w-full bg-slate-50 border border-slate-200 rounded-xl px-4 py-2.5 text-sm focus:ring-2 focus:ring-blue-500 outline-none font-mono" 
                  />
                  <button onClick={() => toggleApiKey(model.id)} className="absolute right-3 top-1/2 -translate-y-1/2 text-slate-400 hover:text-slate-600">
                    {showApiKey[model.id] ? <EyeOff className="w-4 h-4" /> : <Eye className="w-4 h-4" />}
                  </button>
                </div>
              </div>
            </div>

            <div className="mt-6 flex gap-3">
              <button className="flex items-center gap-2 px-4 py-2 bg-slate-100 hover:bg-slate-200 rounded-lg text-sm font-semibold text-slate-600 transition-colors">
                <RefreshCw className="w-4 h-4" /> 验证连接
              </button>
              <button className="flex items-center gap-2 px-4 py-2 hover:bg-slate-50 rounded-lg text-sm font-semibold text-slate-400 transition-colors">
                 官方文档 <ExternalLink className="w-3 h-3" />
              </button>
            </div>
          </div>
        ))}
      </div>
    </div>
  );

  // --- 子组件：图片管理 (已有逻辑) ---
  const ImagesView = () => {
    const mockImages = [
      { id: 1, name: "微信图片_20260205.jpg", url: "https://images.unsplash.com/photo-1519085360753-af0119f7cbe7?auto=format&fit=crop&q=80&w=400", size: "1.2MB" },
      { id: 2, name: "商品详情_A1.png", url: "https://images.unsplash.com/photo-1542291026-7eec264c27ff?auto=format&fit=crop&q=80&w=400", size: "850KB" },
      { id: 3, name: "客服回复图.jpg", url: "https://images.unsplash.com/photo-1523275335684-37898b6baf30?auto=format&fit=crop&q=80&w=400", size: "2.1MB" },
      { id: 4, name: "营销海报_05.jpg", url: "https://images.unsplash.com/photo-1491553895911-0055eca6402d?auto=format&fit=crop&q=80&w=400", size: "1.5MB" },
    ];
    return (
      <div className="flex-1 flex overflow-hidden">
        <div className="flex-1 flex flex-col p-8 overflow-y-auto">
          <div className="flex items-center justify-between mb-8">
            <div>
              <h2 className="text-2xl font-bold text-slate-800">图片素材库</h2>
              <p className="text-slate-500 text-sm">管理 AI 客服在对话中使用的商品图片与素材</p>
            </div>
            <div className="flex gap-3">
              <button className="flex items-center gap-2 bg-slate-100 text-slate-700 px-4 py-2 rounded-xl text-sm font-semibold hover:bg-slate-200 border border-slate-200"><Grid className="w-4 h-4" /> 全选</button>
              <button className="flex items-center gap-2 bg-blue-600 text-white px-5 py-2 rounded-xl text-sm font-semibold hover:bg-blue-700 shadow-lg shadow-blue-200"><Upload className="w-4 h-4" /> 上传新图片</button>
            </div>
          </div>
          <div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6">
            {mockImages.map((img) => (
              <div key={img.id} className={`group relative rounded-2xl bg-white border-2 overflow-hidden cursor-pointer transition-all duration-300 hover:shadow-xl hover:-translate-y-1 border-slate-100 shadow-sm`} onClick={() => setPreviewImage(img)}>
                <div className="aspect-[4/5] overflow-hidden bg-slate-100">
                  <img src={img.url} alt={img.name} className="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500" />
                </div>
                <div className="p-4 bg-white">
                  <p className="text-sm font-semibold text-slate-700 truncate w-full">{img.name}</p>
                  <div className="flex items-center justify-between mt-1">
                    <span className="text-[10px] text-slate-400 font-mono">JPG · {img.size}</span>
                    <span className="text-[10px] bg-slate-100 text-slate-500 px-1.5 py-0.5 rounded uppercase font-bold">已索引</span>
                  </div>
                </div>
              </div>
            ))}
          </div>
        </div>
        
        {/* 图片预览侧滑 */}
        {previewImage && (
          <div className="w-[400px] bg-white border-l border-slate-200 flex flex-col shadow-2xl">
            <div className="p-6 border-b border-slate-100 flex items-center justify-between">
              <h3 className="font-bold text-slate-700 uppercase tracking-wide text-sm flex items-center gap-2"><div className="w-2 h-2 rounded-full bg-blue-500"></div> 素材预览</h3>
              <button onClick={() => setPreviewImage(null)} className="text-slate-400 hover:text-slate-600 p-1 rounded-full hover:bg-slate-200"><ChevronRight className="w-5 h-5" /></button>
            </div>
            <div className="p-8 space-y-8 overflow-y-auto">
              <div className="rounded-2xl overflow-hidden shadow-2xl border border-slate-100"><img src={previewImage.url} alt="preview" className="w-full" /></div>
              <div className="space-y-4">
                <div className="space-y-1"><label className="text-[10px] uppercase font-bold text-slate-400">文件名</label><p className="text-sm font-medium">{previewImage.name}</p></div>
                <div className="space-y-1"><label className="text-[10px] uppercase font-bold text-slate-400">大小</label><p className="text-sm font-medium">{previewImage.size}</p></div>
              </div>
            </div>
          </div>
        )}
      </div>
    );
  };

  return (
    <div className="flex h-screen w-full bg-[#f8f9fb] text-slate-800 font-sans overflow-hidden">
      
      {/* 侧边栏 */}
      <aside className="w-72 bg-[#1a1c1e] flex flex-col shadow-2xl z-20">
        <div className="p-6">
          <h1 className="text-white text-xl font-bold flex items-center gap-2">
            <div className="w-8 h-8 bg-blue-500 rounded-lg flex items-center justify-center"><Settings className="text-white w-5 h-5" /></div>
            AI 智能控制台
          </h1>
          <p className="text-slate-500 text-xs mt-1 ml-10">微信小店自动化助手</p>
        </div>

        <div className="px-4 py-2 space-y-6 flex-1 overflow-y-auto">
          <div className="space-y-2">
            <label className="text-slate-400 text-[10px] uppercase font-bold tracking-wider px-2">快速操作</label>
            <div className="grid grid-cols-2 gap-3">
              <button onClick={() => setIsRunning(!isRunning)} className={`flex items-center justify-center gap-2 py-3 rounded-xl font-medium transition-all ${isRunning ? 'bg-blue-600 text-white shadow-lg shadow-blue-900/40' : 'bg-green-600 hover:bg-green-500 text-white'}`}>
                {isRunning ? <RefreshCw className="w-4 h-4 animate-spin" /> : <Play className="w-4 h-4 fill-current" />}
                {isRunning ? '正在运行' : '启动 AI'}
              </button>
              <button onClick={() => setIsRunning(false)} className="flex items-center justify-center gap-2 bg-[#2a2d31] hover:bg-red-900/30 hover:text-red-400 text-slate-300 py-3 rounded-xl transition-all">
                <Square className="w-4 h-4 fill-current" /> 停止
              </button>
            </div>
            <div className="grid grid-cols-2 gap-3">
              <button className="bg-[#2a2d31] hover:bg-[#35383e] text-slate-300 py-2.5 rounded-xl text-xs transition-all flex items-center justify-center gap-2 border border-white/5"><RefreshCw className="w-3.5 h-3.5" /> 刷新状态</button>
              <button className="bg-[#2a2d31] hover:bg-blue-900/20 hover:text-blue-400 text-slate-300 py-2.5 rounded-xl text-xs transition-all flex items-center justify-center gap-2 border border-white/5"><Target className="w-3.5 h-3.5" /> 测试抓取</button>
            </div>
          </div>

          {/* 系统状态卡片 */}
          <div className="bg-[#2a2d31] rounded-2xl p-4 border border-white/5">
            <div className="flex items-center justify-between mb-2"><span className="text-slate-400 text-xs">系统状态</span><span className="flex items-center gap-1.5 text-green-400 text-xs font-bold uppercase tracking-tight"><div className="w-1.5 h-1.5 rounded-full bg-green-400 animate-pulse"></div> 就绪</span></div>
            <div className="flex justify-between items-end">
              <div><p className="text-2xl font-bold text-white">128</p><p className="text-[10px] text-slate-500 uppercase tracking-wide">今日处理会话</p></div>
              <div className="h-10 w-24 bg-blue-500/10 rounded-lg flex items-end gap-1 px-2 py-1">{[4,7,5,8,6,9,7].map((h, i) => (<div key={i} className="flex-1 bg-blue-500 rounded-t-sm" style={{height: `${h*10}%`}}></div>))}</div>
            </div>
          </div>
        </div>

        {/* 底部运行日志 */}
        <div className="p-4 bg-black/20 mt-auto">
          <div className="flex items-center justify-between text-slate-500 mb-2 px-1">
            <div className="flex items-center gap-2 text-[10px] font-bold uppercase"><Terminal className="w-3 h-3" /> 运行日志</div>
            <button className="text-[10px] hover:text-white transition-colors">查看全部</button>
          </div>
          <div className="bg-black/30 rounded-lg p-2 font-mono text-[10px] text-green-500/80 leading-relaxed overflow-hidden h-20">
            <div className="flex gap-2 opacity-60"><span>[15:52:19]</span> <span>正在加载微信小店...</span></div>
            <div className="flex gap-2 opacity-80"><span>[15:52:20]</span> <span>页面加载完成 ✓</span></div>
            <div className="flex gap-2"><span>[15:52:34]</span> <span className="text-blue-400 text-[9px]">抓取到待处理消息: "姐姐这个发片怎么带?"</span></div>
          </div>
        </div>
      </aside>

      {/* 主内容区 */}
      <main className="flex-1 flex flex-col min-w-0">
        <header className="bg-white border-b border-slate-200 h-16 flex items-center px-8 justify-between shrink-0">
          <nav className="flex gap-1 h-full">
            {[
              {id: 'shop', icon: <ShoppingBag className="w-4 h-4" />, label: '微信小店'},
              {id: 'knowledge', icon: <BookOpen className="w-4 h-4" />, label: '知识库管理'},
              {id: 'model', icon: <Settings className="w-4 h-4" />, label: '模型配置'},
              {id: 'images', icon: <ImageIcon className="w-4 h-4" />, label: '图片管理'},
            ].map(tab => (
              <button key={tab.id} onClick={() => setActiveTab(tab.id)} className={`flex items-center gap-2 px-5 transition-all relative ${activeTab === tab.id ? 'text-blue-600 font-semibold' : 'text-slate-500 hover:text-slate-700'}`}>
                {tab.icon} {tab.label}
                {activeTab === tab.id && <div className="absolute bottom-0 left-0 right-0 h-0.5 bg-blue-600 rounded-full" />}
              </button>
            ))}
          </nav>
          <div className="flex items-center gap-4">
             <div className="bg-blue-50 text-blue-600 px-3 py-1 rounded-full text-xs font-bold border border-blue-100 uppercase tracking-tighter">DeepSeek-V3</div>
          </div>
        </header>

        <section className="flex-1 overflow-hidden bg-[#f8f9fb]">
          {activeTab === 'shop' && <ShopView />}
          {activeTab === 'knowledge' && <KnowledgeView />}
          {activeTab === 'model' && <ModelConfigView />}
          {activeTab === 'images' && <ImagesView />}
        </section>
      </main>
    </div>
  );
};

export default App;